rust fn len(s) {
    match s {
        mold::Value::String(s) => {
            let length = s.value.len();
            mold::Value::Number(mold::Number::from(length as f64))
        }
        _ => panic!()
    }
}

print()